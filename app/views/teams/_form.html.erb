<%= form_with model: [@tournament, @team], local: true do |f| %>
  <div class="mb-3">
    <%= f.label :name, "Nom de l'équipe", class: "form-label" %>
    <%= f.text_field :name, class: "form-control", placeholder: "Ex: Les Tigres" %>
  </div>

  <%= f.submit class: "btn btn-primary" %>
<% end %>
